dynasty_decisions = {

	give_narya_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = narya
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = narya }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = narya }
		}			
		effect = {
			FROM = { remove_trait = narya }
			add_trait = narya
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_nenya_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = nenya
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = nenya }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = nenya }
		}			
		effect = {
			FROM = { remove_trait = nenya }
			add_trait = nenya
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_vilya_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = vilya
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = vilya }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = vilya }
		}			
		effect = {
			FROM = { remove_trait = vilya }
			add_trait = vilya
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_barahir_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = barahir
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = barahir }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = barahir }
		}			
		effect = {
			FROM = { remove_trait = barahir }
			add_trait = barahir
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_arkenstone_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = arkenstone
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = arkenstone }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = arkenstone }
		}			
		effect = {
			FROM = { remove_trait = arkenstone }
			add_trait = arkenstone
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_annunimas_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = annuminas
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = annuminas }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = annuminas }
		}			
		effect = {
			FROM = { remove_trait = annuminas }
			add_trait = annuminas
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_horngondor_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = horngondor
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = horngondor }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = horngondor }
		}			
		effect = {
			FROM = { remove_trait = horngondor }
			add_trait = horngondor
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_stewardscept_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = stewardscept
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = stewardscept }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = stewardscept }
		}			
		effect = {
			FROM = { remove_trait = stewardscept }
			add_trait = stewardscept
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_durin_ring_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = durin_ring
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = durin_ring }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = durin_ring }
		}			
		effect = {
			FROM = { remove_trait = durin_ring }
			add_trait = durin_ring
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_dwarf_ring_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = dwarf_ring
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = dwarf_ring }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = dwarf_ring }
		}			
		effect = {
			FROM = { remove_trait = dwarf_ring }
			add_trait = dwarf_ring
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_lesser_ring_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = lesser_ring
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = lesser_ring }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = lesser_ring }
		}			
		effect = {
			FROM = { remove_trait = lesser_ring }
			add_trait = lesser_ring
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_mithril_armor_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = mithril_armor
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = mithril_armor }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = mithril_armor }
		}			
		effect = {
			FROM = { remove_trait = mithril_armor }
			add_trait = mithril_armor
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_himling_armour_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = himling_armour
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = himling_armour }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = himling_armour }
		}			
		effect = {
			FROM = { remove_trait = himling_armour }
			add_trait = himling_armour
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_arvedui_armor_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = arvedui_armor
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = arvedui_armor }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = arvedui_armor }
		}			
		effect = {
			FROM = { remove_trait = arvedui_armor }
			add_trait = arvedui_armor
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	give_erebor_armor_to_heir = {
		is_high_prio = yes
		from_potential = {
			ai = no
			is_ruler = yes
			trait = erebor_armor
		}
		potential = {
			is_child_of = FROM
			NOT = { trait = erebor_armor }
		}
		allow = {
			FROM = { war = no }
			FROM = { NOT = { trait = incapable } }
			FROM = { trait = erebor_armor }
		}			
		effect = {
			FROM = { remove_trait = erebor_armor }
			add_trait = erebor_armor
		}
		ai_will_do = {
			factor = 0
		}
	}
}