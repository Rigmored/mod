decisions = {
	#Steal the One Ring#
	steal_one_ring = {
		is_high_prio = yes
		potential = {
			ai = no
			any_courtier = {
				prisoner = yes
				has_artifact = artifact_ring
			}
		}
		allow = {
			mep_is_adult = yes
			in_command = no
			prisoner = no
			NOT = { trait = blinded }
			NOT = { trait = craven }
			NOT = { trait = imbecile }
			NOT = { trait = weak }
			NOT = { year = 11363 }
			NOT = { trait = incapable }
		}
		effect = {
			ROOT = {
				character_event = { id = stealonering.0 }
			}
		}
	}
}
